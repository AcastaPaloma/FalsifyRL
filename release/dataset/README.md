---
license: mit
language:
  - en
pretty_name: FalsifyRL Reward-Hacking Falsification
size_categories:
  - 1K<n<10K
task_categories:
  - text-classification
  - text-generation
tags:
  - reinforcement-learning
  - multi-agent
  - robotics
  - reward-hacking
  - synthetic
configs:
  - config_name: default
    data_files:
      - split: train
        path: train.jsonl
      - split: validation
        path: validation.jsonl
      - split: test
        path: test.jsonl
---

# FalsifyRL Reward-Hacking Falsification

FalsifyRL is a synthetic, executable benchmark for identifying and repairing proxy-reward failures
in embodied multi-agent reinforcement learning.

Each example contains:

- a natural-language task specification,
- a declarative reward program,
- a compact two-agent episode trace,
- a strict JSON diagnosis with evidence, responsible agents, counterexample configuration, and an
  executable reward patch.

## Dataset design

The dataset contains 3,840 examples arranged into 1,920 matched pairs. Both examples in a pair share
the exact reward program and scenario seed: one trace completes the task safely and the other
exploits the reward. This blocks a reward-code-only shortcut and makes episode evidence necessary.

| Split | Scenario families | Examples |
| --- | --- | ---: |
| train | `dual_arm_workspace`, `warehouse_handoff` | 2,560 |
| validation | `cooperative_transport` | 640 |
| test | `crossing_navigation` | 640 |

The verdict classes are exactly balanced: 1,920 aligned and 1,920 reward-hack cases.

## Defect taxonomy

- free riding
- collision blindness
- destructive interference
- unsafe speed
- out-of-bounds reward
- no-op/survival bonus
- premature completion
- multi-agent credit misattribution

## Verification

Gold labels are generated from mutation metadata and an independent executable task oracle, not
from a language model. Every exploit must:

1. fail the true task validator,
2. earn at least 90% of the aligned reference return,
3. include an executable declarative patch,
4. lose at least 0.5 return after patching,
5. rank at least 0.5 below aligned behavior after patching.

`manifest.json` records exact SHA-256 hashes, split counts, generator version, and verification
status. CSV files contain only `prompt` and `completion` for direct AutoScientist import. JSONL files
also include non-training provenance and evaluation metadata.

## Intended use

- supervised adaptation of reward-audit assistants,
- held-out evaluation of structured diagnosis and repair,
- research on reward specification and multi-agent credit failures,
- demonstrations of executable synthetic-data verification.

This is not a robot-control policy and should not be used as an autonomous safety authority.
Predicted patches must be executed in a simulator and reviewed before deployment.

## Limitations

- Scenarios are compact declarative abstractions, not high-fidelity physics.
- Version 1 contains exactly two agents.
- Metrics and reward mutations cover a deliberately bounded taxonomy.
- Trace language and reward pseudocode are synthetic and may not match every production codebase.
- The held-out split measures scenario-family transfer within the same generator.

## License

MIT. The dataset is generated by the FalsifyRL codebase and contains no personal data or scraped
content.

